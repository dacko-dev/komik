import { Modak, Pacifico, Smooch_Sans } from 'next/font/google'

export const smoochSans = Smooch_Sans({
    variable: '--font-smooch-sans',
    subsets: ['latin'],
})

export const modak = Modak({
    variable: '--font-modak',
    subsets: ['latin'],
    weight: ['400'],
})

export const pacifico = Pacifico({
    variable: '--font-pacifico',
    subsets: ['latin'],
    weight: ['400'],
})
